repos:
  - repo: local
    hooks:
      - id: detect-secrets
        name: Detect Secrets
        entry: detect-secrets-hook --baseline .secrets.baseline
        language: system
        types: [python, yaml, shell, text]